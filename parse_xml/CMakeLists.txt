INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

FILE(GLOB XML_SRC           *.h *.cc *.c)
SOURCE_GROUP("xml"          FILES ${XML_SRC})

ADD_EXECUTABLE(xml
  ${XML_SRC}
  )
TARGET_LINK_LIBRARIES(xml antlr3c)
