INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

FILE(GLOB EXPR_SRC          *.h *.cc *.c)
SOURCE_GROUP("expr"         FILES ${EXPR_SRC})

ADD_EXECUTABLE(expr
  ${EXPR_SRC}
  )
TARGET_LINK_LIBRARIES(expr antlr3c)
